export const metadata = { title: "Org Overview" };

export default function OrgDashboardPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-[var(--foreground)]">Organisation overview</h1>
      <p className="mt-2 text-sm text-[var(--muted-foreground)]">
        Drive metrics and management — wired in Phase 3.
      </p>
    </div>
  );
}
